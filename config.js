/* MovieFlix V3 — Supabase connection
   Paste ONLY your Project URL and PUBLISHABLE key here.
   Never put a service_role / secret key in browser code. */
window.MOVIEFLIX_CONFIG = {
  supabaseUrl: '',
  publishableKey: ''
};
